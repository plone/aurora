Fixed the `check-vite-optimize-deps` audit script to ignore entries declared only in `optimizeDeps.exclude`, to correctly skip type-only named re-exports, and to check `ssr.optimizeDeps.include` separately for dependencies used only in `*.server.*` files. @sneridagh
