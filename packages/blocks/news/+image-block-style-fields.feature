The image block now stores its alignment and size as schema-driven style fields, ships its styles as a CSS module instead of a global side-effect import, floats left/right aligned images so following content wraps around them (including list markers), couples alignment with the image size and block width, keeps floated images selectable in the editor, and can link the image to another page. @sneridagh
